---
import BaseLayout from '@/layouts/BaseLayout.astro';
import DecorativeBackground from '@/components/DecorativeBackground.astro';
import LanguageSwitcher from '@/components/LanguageSwitcher.astro';
import ThemeToggle from '@/components/ThemeToggle.astro';
import Icon from '@/components/Icon.astro';
import { translations, type Language } from '@/i18n/translations';

const lang = (Astro.url.searchParams.get('lang') as Language) || 'es';
const t = (key: string) => translations[lang]?.[key] || translations['es']?.[key] || key;
const currentPath = Astro.url.pathname;
---

  <BaseLayout>
    <DecorativeBackground />
    <LanguageSwitcher currentPath={currentPath} altLang={lang === 'es' ? 'en' : 'es'} />
    <ThemeToggle />

    <!-- Admin Trainer Preview Switcher Bar -->
    <div id="trainer-admin-preview-banner" class="hidden fixed top-3 left-3 z-50">
      <a
        href={`/admin/dashboard?lang=${lang}`}
        class="inline-flex items-center gap-2 rounded-xl border border-purple-500/30 bg-purple-950/80 px-3 py-1.5 text-xs font-medium text-purple-300 backdrop-blur-md shadow-lg transition-all hover:bg-purple-900/80 active:scale-95"
      >
        <span class="flex h-2 w-2 rounded-full bg-purple-400 animate-pulse"></span>
        <span>👑 Modo Trainer (Admin)</span>
        <span class="text-zinc-400">|</span>
        <span class="text-purple-200 underline">Volver al Admin</span>
      </a>
    </div>

    <!-- Page content -->
    <slot />

<<<<<<< HEAD
<script>
  import { auth, db } from '@/lib/firebase';
  import { onAuthStateChanged } from 'firebase/auth';
  import { doc, getDoc, onSnapshot, query, where, collection } from 'firebase/firestore';
  import { showToast } from '@/lib/shared/ui';
  import { logger } from '@/lib/shared/logger';

  let unsubMessages: (() => void) | null = null;
  let unsubAuth: (() => void) | null = null;

  unsubAuth = onAuthStateChanged(auth, async (user) => {
    if (!user) return;
    try {
      const email = (user.email || '').toLowerCase();
      const isBootstrapAdmin = email === 'servicioweb.pmi@gmail.com' || email === 'sevicioweb.pmi@gmail.com';

      const userDocSnap = await getDoc(doc(db, 'users', user.uid));
      const role = userDocSnap.data()?.role;

      if (role === 'admin' || isBootstrapAdmin) {
        document.getElementById('trainer-admin-preview-banner')?.classList.remove('hidden');
      }

      // Limpiar suscripción anterior si existe
      if (unsubMessages) {
        unsubMessages();
        unsubMessages = null;
      }

      // Suscribirse a mensajes no leídos en tiempo real
      const messagesRef = collection(db, 'messages');
      const q = query(
        messagesRef,
        where('receiverId', '==', user.uid),
        where('isRead', '==', false)
      );

      unsubMessages = onSnapshot(q, (snapshot) => {
        const unreadCount = snapshot.size;
        const badge = document.getElementById('trainer-chat-badge');

        if (unreadCount > 0) {
          badge?.classList.remove('hidden');
          if (badge) {
            badge.textContent = unreadCount > 99 ? '99+' : unreadCount.toString();
          }

          const isOnChatPage = window.location.pathname.includes('/trainer/chat');
          if (!isOnChatPage && snapshot.docChanges().some(change => change.type === 'added') && snapshot.docs.length > 0) {
            const latestMsg = snapshot.docs[0]?.data();
            if (latestMsg) {
              showToast({
                message: `Nuevo mensaje de ${latestMsg.senderId === user.uid ? 'ti' : 'un cliente'}`,
                type: 'info',
                duration: 3000,
                position: 'bottom'
              });
            }
          }
        } else {
          badge?.classList.add('hidden');
        }
      });
    } catch (e) {
      logger.error('TrainerLayout', 'Error:', e);
    }
  });

  // Cleanup SPA navigation
  document.addEventListener('astro:before-swap', () => {
    unsubAuth?.();
    unsubMessages?.();
  });

  // Fallback cleanup for traditional navigation
  window.addEventListener('beforeunload', () => {
    unsubMessages?.();
    unsubAuth?.();
  });
</script>

    <!-- Bottom Navigation Trainer -->
  <nav class="fixed bottom-0 left-0 right-0 z-40 border-t border-zinc-800/60 bg-zinc-900/90 backdrop-blur-xl">
=======
  <!-- Bottom Navigation Trainer -->
  <nav class="fixed bottom-0 left-0 right-0 z-40 border-t theme-border theme-surface/90 backdrop-blur-xl">
>>>>>>> 4042d86ac520c28484786564a781e3d6e901af5a
    <div class="mx-auto flex max-w-lg items-center justify-around px-2 py-2">
      <a
        href={`/trainer/dashboard?lang=${lang}`}
        class={`flex flex-col items-center gap-0.5 px-3 py-1.5 rounded-xl transition-all ${
          currentPath === '/trainer/dashboard' || currentPath === '/trainer/dashboard/'
            ? 'theme-text-brand'
            : 'theme-text-tertiary hover:theme-text-secondary'
        }`}
      >
        <Icon name="home" class="h-5 w-5" />
        <span class="text-[10px] font-medium">{t('trainer.dashboard')}</span>
      </a>

      <a
        href={`/trainer/clients?lang=${lang}`}
        class={`flex flex-col items-center gap-0.5 px-3 py-1.5 rounded-xl transition-all ${
          currentPath.startsWith('/trainer/clients')
            ? 'theme-text-brand'
            : 'theme-text-tertiary hover:theme-text-secondary'
        }`}
      >
        <Icon name="users" class="h-5 w-5" />
        <span class="text-[10px] font-medium">{t('trainer.clients')}</span>
      </a>

      <a
        href={`/trainer/workouts?lang=${lang}`}
        class={`flex flex-col items-center gap-0.5 px-3 py-1.5 rounded-xl transition-all ${
          currentPath.startsWith('/trainer/workouts')
            ? 'theme-text-brand'
            : 'theme-text-tertiary hover:theme-text-secondary'
        }`}
      >
        <Icon name="workouts" class="h-5 w-5" />
        <span class="text-[10px] font-medium">{t('trainer.workouts')}</span>
      </a>

      <a
        href={`/trainer/diets?lang=${lang}`}
        class={`flex flex-col items-center gap-0.5 px-3 py-1.5 rounded-xl transition-all ${
          currentPath.startsWith('/trainer/diets')
            ? 'theme-text-brand'
            : 'theme-text-tertiary hover:theme-text-secondary'
        }`}
      >
        <Icon name="diets" class="h-5 w-5" />
        <span class="text-[10px] font-medium">{t('trainer.diets')}</span>
      </a>

      <a
        href={`/trainer/chat?lang=${lang}`}
        class={`flex flex-col items-center gap-0.5 px-3 py-1.5 rounded-xl transition-all relative ${
          currentPath.startsWith('/trainer/chat')
            ? 'theme-text-brand'
            : 'theme-text-tertiary hover:theme-text-secondary'
        }`}
      >
        <div class="relative">
          <Icon name="chat" class="h-5 w-5" />
          <span id="trainer-chat-badge" class="hidden absolute -top-1.5 -right-2 flex h-4 w-4 items-center justify-center rounded-full bg-emerald-500 text-[9px] font-bold text-white shadow-lg">0</span>
        </div>
        <span class="text-[10px] font-medium">{t('trainer.chat')}</span>
      </a>

      <a
        href={`/trainer/settings?lang=${lang}`}
        class={`flex flex-col items-center gap-0.5 px-3 py-1.5 rounded-xl transition-all ${
          currentPath.startsWith('/trainer/settings')
            ? 'theme-text-brand'
            : 'theme-text-tertiary hover:theme-text-secondary'
        }`}
      >
        <Icon name="settings" class="h-5 w-5" />
        <span class="text-[10px] font-medium">Config</span>
      </a>
    </div>
  </nav>
</BaseLayout>
