---
<<<<<<< HEAD
import BaseLayout from '@/layouts/BaseLayout.astro';
import { translations, type Language } from '@/i18n/translations';

export interface Props {
  /** Ruta actual para el language switcher */
  currentPath: string;
  /** Idioma actual */
  lang?: Language;
}

const { currentPath } = Astro.props;
const lang = (Astro.url.searchParams.get('lang') as Language) || 'es';
const t = (key: string) => translations[lang]?.[key] || translations['es']?.[key] || key;
const altLang = lang === 'es' ? 'en' : 'es';
---

<BaseLayout>
  <div class="flex min-h-screen flex-col items-center justify-center px-4 py-12">
    <!-- Fondo decorativo -->
    <div class="pointer-events-none fixed inset-0 overflow-hidden">
      <div class="absolute -left-40 -top-40 h-80 w-80 rounded-full bg-emerald-500/5 blur-3xl"></div>
      <div class="absolute -bottom-40 -right-40 h-80 w-80 rounded-full bg-emerald-500/5 blur-3xl"></div>
    </div>

    <!-- Top Bar Navigation & Language Switcher -->
    <div class="fixed left-4 top-4 z-50">
      <div id="top-session-bar" class="hidden">
        <!-- Rendered by JS if logged in -->
      </div>
    </div>

    <div class="fixed right-4 top-4 z-50 flex items-center gap-2">
      <div id="top-panel-btn" class="hidden">
        <!-- Rendered by JS if logged in -->
      </div>
      <a
        href={`${currentPath}?lang=${altLang}`}
        class="inline-flex items-center gap-1.5 rounded-xl border border-zinc-700/50 bg-zinc-900/80 px-3 py-2 text-xs font-medium text-zinc-400 backdrop-blur-sm transition-all hover:border-zinc-600 hover:text-zinc-200"
      >
        <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 21a9.004 9.004 0 0 0 8.716-6.747M12 21a9.004 9.004 0 0 1-8.716-6.747M12 21c2.485 0 4.5-4.03 4.5-9S14.485 3 12 3m0 18c-2.485 0-4.5-4.03-4.5-9S9.515 3 12 3m0 0a8.997 8.997 0 0 1 7.843 4.582M12 3a8.997 8.997 0 0 0-7.843 4.582m15.686 0A11.953 11.953 0 0 1 12 10.5c-2.998 0-5.74-1.1-7.843-2.918m15.686 0A8.959 8.959 0 0 1 21 12c0 .778-.099 1.533-.284 2.253m0 0A17.919 17.919 0 0 1 12 16.5c-3.162 0-6.133-.815-8.716-2.247m0 0A9.015 9.015 0 0 1 3 12c0-1.605.42-3.113 1.157-4.418" />
        </svg>
        {altLang === 'en' ? 'EN' : 'ES'}
      </a>
    </div>

<script>
  import { auth, db } from '@/lib/firebase';
  import { onAuthStateChanged } from 'firebase/auth';
  import { doc, getDoc } from 'firebase/firestore';

  onAuthStateChanged(auth, async (user) => {
    const topBar = document.getElementById('top-session-bar');
    const panelBtn = document.getElementById('top-panel-btn');
    if (!topBar || !panelBtn) return;

    if (user) {
      let role = 'client';
      let name = user.displayName || user.email || 'Usuario';

      try {
        const docSnap = await getDoc(doc(db, 'users', user.uid));
        if (docSnap.exists()) {
          const data = docSnap.data();
          role = data.role || 'client';
          name = data.name || name;
        }
      } catch (e) {
        console.error('Error fetching role in PublicPageLayout:', e);
      }

      const email = (user.email || '').toLowerCase();
      if (email === 'servicioweb.pmi@gmail.com' || email === 'sevicioweb.pmi@gmail.com') {
        role = 'admin';
      }

      let dashboardUrl = '/client/dashboard';
      let roleLabel = 'Cliente';
      if (role === 'admin') {
        dashboardUrl = '/admin/dashboard';
        roleLabel = 'Admin';
      } else if (role === 'trainer') {
        dashboardUrl = '/trainer/dashboard';
        roleLabel = 'Trainer';
      }

      topBar.classList.remove('hidden');
      topBar.innerHTML = `
        <div class="inline-flex items-center gap-2 rounded-xl border border-emerald-500/20 bg-zinc-900/90 px-3 py-1.5 text-xs font-medium text-emerald-400 backdrop-blur-sm">
          <span class="h-2 w-2 rounded-full bg-emerald-400 animate-pulse"></span>
          <span class="text-zinc-200 hidden sm:inline">${name}</span>
          <span class="rounded-full bg-emerald-500/10 px-2 py-0.5 text-[10px] text-emerald-400 border border-emerald-500/20">${roleLabel}</span>
        </div>
      `;

      panelBtn.classList.remove('hidden');
      panelBtn.innerHTML = `
        <a href="${dashboardUrl}" class="inline-flex items-center gap-1.5 rounded-xl border border-emerald-500/30 bg-emerald-500/10 px-3 py-2 text-xs font-semibold text-emerald-400 backdrop-blur-sm transition-all hover:bg-emerald-500/20">
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6A2.25 2.25 0 0 1 6 3.75h2.25A2.25 2.25 0 0 1 10.5 6v2.25a2.25 2.25 0 0 1-2.25 2.25H6a2.25 2.25 0 0 1-2.25-2.25V6ZM3.75 15.75A2.25 2.25 0 0 1 6 13.5h2.25a2.25 2.25 0 0 1 2.25 2.25V18a2.25 2.25 0 0 1-2.25 2.25H6A2.25 2.25 0 0 1 3.75 18v-2.25ZM13.5 6a2.25 2.25 0 0 1 2.25-2.25H18A2.25 2.25 0 0 1 20.25 6v2.25A2.25 2.25 0 0 1 18 10.5h-2.25a2.25 2.25 0 0 1-2.25-2.25V6ZM13.5 15.75a2.25 2.25 0 0 1 2.25-2.25H18a2.25 2.25 0 0 1 2.25 2.25V18A2.25 2.25 0 0 1 18 20.25h-2.25A2.25 2.25 0 0 1 13.5 18v-2.25Z" />
          </svg>
          <span>Mi Panel</span>
        </a>
      `;
    }
  });
</script>

    <slot />
  </div>
</BaseLayout>
=======
/**
 * Layout para páginas públicas (login, register, recover, index).
 * Centraliza: fondo decorativo, language switcher, theme toggle y lógica i18n.
 *
 * @slot - Contenido de la página
 */

import BaseLayout from '@/layouts/BaseLayout.astro';
import DecorativeBackground from '@/components/DecorativeBackground.astro';
import LanguageSwitcher from '@/components/LanguageSwitcher.astro';
import { translations, type Language } from '@/i18n/translations';

const lang = (Astro.url.searchParams.get('lang') as Language) || 'es';
const t = (key: string) => translations[lang]?.[key] || translations['es']?.[key] || key;
const altLang = lang === 'es' ? 'en' : 'es';
const currentPath = Astro.url.pathname;
---

<BaseLayout>
  <DecorativeBackground />
  <LanguageSwitcher currentPath={currentPath} altLang={altLang} />
  <div class="pb-20">
    <slot />
  </div>
</BaseLayout>
>>>>>>> 4042d86ac520c28484786564a781e3d6e901af5a
